export enum AiFrameJobStatus {
  QUEUED = 'queued',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  ACCEPTED = 'accepted',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
  DELETED = 'deleted',
}
